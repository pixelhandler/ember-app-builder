'use-strict';

module.exports = App.AboutRoute = Ember.Route.extend({
  //model: function () {
    //return this.store.find('post');
  //},
  //renderTemplate: function(controller, model) {
    //this.render('about');
    //this.render('recent', { outlet: 'recent' });
  //}
});
