{{#each post in controller}}
  <div class="Blog-excerpt">
    <h3 class="Blog-excerpt-title u-yellowTxt">{{#link-to 'post' post}}{{post.title}}{{/link-to}}</h3>
    <section class="Blog-excerpt-summary">
      {{post.excerpt}}
    </section>
    {{#link-to "post" post class="u-button"}}Read on →{{/link-to}}
  </div>
{{/each}}
{{outlet}}
